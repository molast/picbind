import React from "react";
import { getLang,getWorkspaceLabels } from "../../locales";
import type { WorkspaceImage } from "../types";
const text=(key:string)=>getWorkspaceLabels(getLang())[key]||key;
export function WorkspaceDeleteDialog({image,onClose,onConfirm}:{image?:WorkspaceImage|null;onClose():void;onConfirm():void}){return image?<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4" onMouseDown={(event)=>event.target===event.currentTarget&&onClose()}><div className="w-full max-w-md rounded-md bg-white p-5 shadow-xl" role="dialog" aria-modal="true"><h2 className="text-lg font-semibold">{text("deleteImage")}</h2><p className="mt-2 truncate text-sm text-slate-500">{image.name}</p><p className="mt-4 text-sm text-slate-600">{text("removeHistory")}</p><div className="mt-5 flex justify-end gap-2"><button type="button" onClick={onClose} className="h-9 rounded-md border px-4 text-sm">{text("cancel")}</button><button type="button" onClick={onConfirm} className="h-9 rounded-md bg-red-600 px-4 text-sm font-semibold text-white">{text("confirm")}</button></div></div></div>:null;}
