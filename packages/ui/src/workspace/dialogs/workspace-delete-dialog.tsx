import React from "react";
import { getLang,getWorkspaceLabels } from "../../locales";
import type { WorkspaceImage } from "../types";
const text=(key:string)=>getWorkspaceLabels(getLang())[key]||key;
export function WorkspaceDeleteDialog({image,choice,onChoiceChange,onClose,onConfirm}:{image?:WorkspaceImage|null;choice:"library"|"permanent";onChoiceChange(choice:"library"|"permanent"):void;onClose():void;onConfirm():void}){return image?<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4" onMouseDown={(event)=>event.target===event.currentTarget&&onClose()}><div className="w-full max-w-md rounded-md bg-white p-5 shadow-xl" role="dialog" aria-modal="true"><h2 className="text-lg font-semibold">{text("deleteImage")}</h2><p className="mt-2 truncate text-sm text-slate-500">{image.name}</p><div className="mt-4 grid gap-2"><label className="flex cursor-pointer gap-3 rounded-md border p-3 text-sm"><input type="radio" checked={choice==="library"} onChange={()=>onChoiceChange("library")}/><span><strong className="block">{text("returnToLibrary")}</strong><span className="text-xs text-slate-500">{text("keepOriginal")}</span></span></label><label className="flex cursor-pointer gap-3 rounded-md border p-3 text-sm"><input type="radio" checked={choice==="permanent"} onChange={()=>onChoiceChange("permanent")}/><span><strong className="block">{text("deletePermanently")}</strong><span className="text-xs text-slate-500">{text("removeHistory")}</span></span></label></div><div className="mt-5 flex justify-end gap-2"><button type="button" onClick={onClose} className="h-9 rounded-md border px-4 text-sm">{text("cancel")}</button><button type="button" onClick={onConfirm} className="h-9 rounded-md bg-red-600 px-4 text-sm font-semibold text-white">{text("confirm")}</button></div></div></div>:null;}
