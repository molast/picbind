[package]
name = "picbind-network"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
picbind-protocol = { path = "../picbind-protocol" }

[dev-dependencies]
serde_json = "1"
