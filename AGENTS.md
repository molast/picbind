# AI Agent 项目指令

本文档用于约束参与本仓库开发和维护的 AI 编程助手。

## 适用范围

本文档中的规则适用于整个仓库。若某个子目录中存在范围更具体的 `AGENTS.md`，则该子目录优先遵循更具体的规则。

## 压缩算法文档同步规则

根目录下的 `COMPRESSION_ALGORITHM.md` 是当前图片压缩流程的实现说明文档，必须与仓库中的实际代码保持一致。

任何会改变图片压缩行为的任务，都必须在同一批改动中同步更新 `COMPRESSION_ALGORITHM.md`，不得将文档更新推迟到后续任务。

以下改动必须同步更新算法文档，包括但不限于：

- 新增、删除、替换或升级图片编码器、解码器。
- 引入新的压缩算法，或者使用更优秀的算法替换现有实现。
- 修改 JPEG、PNG、WebP、AVIF 或后续新增格式的编码流程。
- 修改图片特征提取、特征分析、Planner 策略、候选参数生成或编码器选择逻辑。
- 修改 Compression Gain（`K`）的公式、范围、默认值、环境变量或格式专用行为。
- 修改质量指标、Butteraugli、感知质量阈值、质量护栏或候选结果选择逻辑。
- 修改 Alpha 处理、颜色量化、色度采样、抖动、调色板生成或元数据处理。
- 修改同格式文件不增大保护、返回原图策略、Canvas 回退或错误处理。
- 修改文件大小限制、压缩并发、Worker 生命周期、内存清理或算法流程中已经记录的其他行为。
- 新增输入或输出图片格式。

## 算法文档编写规则

更新 `COMPRESSION_ALGORITHM.md` 时必须遵守：

1. 只描述已经实际实现的行为，不能把未来设计或计划写成已完成能力。
2. 保证总体调用链、编码器矩阵、各格式流程、回退规则、环境变量、当前边界和关键实现文件索引与代码一致。
3. 明确区分默认启用、可选启用、失败回退和未来规划的能力。
4. 替换旧算法时，必须删除或修订失效的说明，不能在当前流程中留下互相冲突的历史描述。
5. 记录重要兼容规则，尤其是同格式结果不增大和 Alpha 透明度保护。
6. 如果依赖版本会实质影响算法，应同时更新算法文档和对应的依赖清单。

## 压缩改动检查清单

完成任何压缩相关任务前，需要检查所有适用项目：

- 实现代码与生成的 WASM 文件保持一致。
- 相关 Rust 测试和 TypeScript 检查通过。
- 同格式压缩结果不小于原图时仍然返回原图，除非产品需求明确要求改变该规则。
- 除非明确允许丢失 Alpha，否则不能静默将透明图片压平。
- 单个候选编码失败时，不能错误丢弃其他有效候选。
- 临时 `ImageBitmap`、Blob URL、Worker 和 WASM 资源得到正确释放。
- 新增公开压缩配置时，同步更新 `apps/web/.env.example`。
- `COMPRESSION_ALGORITHM.md` 已反映最终实现。

如果只修改 `COMPRESSION_ALGORITHM.md` 文档，则不需要重新编译 WASM。如果修改了压缩算法、Rust 代码或 WASM API，必须使用仓库规定的 WASM 构建流程，并同步提交 Web 应用所依赖的生成文件。
