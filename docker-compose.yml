version: "3.9"

services:
  web:
    container_name: pixwasm-web
    image: registry.cn-shanghai.aliyuncs.com/molast/nano-img:v1.0.4
    environment:
      - PORT=1997
    ports:
      - "1997:1997"
    restart: unless-stopped
